import { FC } from 'react';
import { PropsWithChildrenOnly } from '../../../types/props-with-children-only';
import { View } from 'native-base';

const SettingsScreen: FC<PropsWithChildrenOnly> = () => {
  return <View></View>;
};

export default SettingsScreen;
